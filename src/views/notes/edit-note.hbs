<div class="col-md-4 mx-auto">
    <div class="card">
        <div class="card-header">
            <h1>Edit note</h1>
        </div>
        <div class="card-body">
            <form action="/notes/edit-note/{{note._id}}?_method=PUT" method="POST">
            <!-- ESTE INPUT SIRVE SIMPLEMENTE PARA AVISARLE AL SERVIDOR QUE QUEREMOS HACER UN UPDATE O UN DELETE
            AL USUARIO NO SE LE VA A MOSTRAR, SIMPLEMENTE SIRVE PARA EL FUNCIONAMIENTO INTERNO DE NUESTRO PROGRAMA-->
            <input type="hidden" name="_method" value="PUT">
                <div class="form-group">
                    <input autofocus type="text" name="title" class="form-control" placeholder="Title"
                        value="{{note.title}}">
                </div>
                <div class="form-group mt-2">
                    <textarea name="description" class="form-control" placeholder="Description" >{{note.description}}</textarea>
                </div>
                <div class="d-grid gap-2 mt-2">
                    <button class="btn btn-primary " type="submit">
                        Update
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>